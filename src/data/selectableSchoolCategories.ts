import { schoolBoardCategories } from "@/features/notices/config/schoolBoardCategories";

export const selectableSchoolCategories = schoolBoardCategories;
export const selectableSchoolCategoryKeys = schoolBoardCategories.map((category) => category.key);
